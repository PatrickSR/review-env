{
  "name": "review-env",
  "private": true,
  "packageManager": "npm@10.9.2",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo dev --filter=@review-env/server",
    "build": "turbo build"
  },
  "devDependencies": {
    "turbo": "^2.4.0"
  }
}
