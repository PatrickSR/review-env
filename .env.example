# GitLab (global - single instance)
GITLAB_URL=https://gitlab.internal

# Service
PORT=3333
MAX_CONTAINERS=20
CONTAINER_TIMEOUT_HOURS=4

# Container resource limits
CONTAINER_CPU_LIMIT=2
CONTAINER_MEMORY_LIMIT=4g

# Docker (macOS Colima users need to set DOCKER_SOCK)
# DOCKER_SOCK=/Users/<user>/.colima/default/docker.sock

# Docker host IP for proxy (default: 127.0.0.1, use host.docker.internal when running in container)
# DOCKER_HOST_IP=127.0.0.1

# Test container timeout (minutes)
# TEST_CONTAINER_TIMEOUT_MINUTES=30
