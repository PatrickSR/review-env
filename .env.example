# GitLab
GITLAB_URL=https://gitlab.internal
GITLAB_PAT=glpat-xxxxx
GITLAB_PROJECT_ID=42
GITLAB_PROJECT_PATH=group/subgroup/project-name
GIT_USER_NAME=review-bot
GIT_USER_EMAIL=review-bot@company.com
WEBHOOK_SECRET=your-webhook-secret

# Service
PORT=3000
MAX_CONTAINERS=20
CONTAINER_TIMEOUT_HOURS=4

# Container
APP_PORTS=3000,5173,8080    # 容器内需要映射的端口列表（逗号分隔），由 Docker 随机分配宿主机端口
REVIEW_IMAGE=review-env:latest
CONTAINER_CPU_LIMIT=2
CONTAINER_MEMORY_LIMIT=4g

# Docker (macOS Colima 用户需要设置 DOCKER_SOCK 指向 Colima socket)
# DOCKER_SOCK=/Users/<user>/.colima/default/docker.sock

# Claude Code
ANTHROPIC_AUTH_TOKEN=
ANTHROPIC_BASE_URL=
ANTHROPIC_MODEL=
